import { BaseComponent, BaseComponentOptions, VoiceCallTapContract, CallingCallTapState } from '@signalwire/core';
/**
 * Instances of this class allow you to control (e.g., resume) the
 * tap inside a Voice Call. You can obtain instances of this class by
 * starting a Tap from the desired {@link Call} (see
 * {@link Call.tap})
 */
export interface CallTap extends VoiceCallTapContract {
}
export declare type CallTapEventsHandlerMapping = {};
export interface CallTapOptions extends BaseComponentOptions<CallTapEventsHandlerMapping> {
}
export declare class CallTapAPI extends BaseComponent<CallTapEventsHandlerMapping> implements VoiceCallTapContract {
    callId: string;
    nodeId: string;
    controlId: string;
    state: CallingCallTapState;
    get id(): string;
    stop(): Promise<this>;
    ended(): Promise<this>;
}
export declare const createCallTapObject: (params: CallTapOptions) => CallTap;
//# sourceMappingURL=CallTap.d.ts.map